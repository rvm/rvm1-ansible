---
- hosts: localhost
  remote_user: travis
  sudo: false

  vars:
    rvm1_rubies:
      - 'ruby-2.1.0'
      - 'ruby-2.1.2'
    rvm1_user: 'travis'
    rvm1_group: 'travis'
    rvm1_temp_download_path: '/tmp'
    rvm1_install_path: '~/.rvm'

  roles:
    - ansible-ruby